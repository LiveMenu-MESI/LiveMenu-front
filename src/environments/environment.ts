/**
 * Entorno de desarrollo.
 * La URL del API se puede sobrescribir con la variable de entorno API_URL al construir.
 */
export const environment = {
  production: false,
  /** Base URL del backend (sin /api/v1). Variable de entorno: API_URL */
  apiUrl: 'https://api.naing.co',
};
