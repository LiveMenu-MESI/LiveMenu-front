services:
  frontend:
    build: .
    ports:
      - "4200:80"
    # Para conectar con un backend en otro servicio:
    # environment:
    #   - API_BASE_URL=http://backend:3000/api
    # depends_on:
    #   - backend

  # Ejemplo de backend (descomentar y crear Dockerfile en backend si aplica):
  # backend:
  #   image: node:20-alpine
  #   working_dir: /app
  #   volumes:
  #     - ../backend:/app
  #   command: npm run start
  #   ports:
  #     - "3000:3000"
